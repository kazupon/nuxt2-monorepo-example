{
  "name": "nuxt2-monorepo-sample",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "lint": "npm run lint --workspaces",
    "format": "prettier --check --ignore-path .gitignore ."
  }
}
